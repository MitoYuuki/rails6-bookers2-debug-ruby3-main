<!-- followers.html.erb -->
<h1>フォロワー一覧</h1>
<% if @users.present? %>
  <ul>
    <% @users.each do |user| %>
      <li><%= link_to user.name, user %></li>
    <% end %>
  </ul>
<% else %>
  <p>フォロワーはいません。</p>
<% end %>