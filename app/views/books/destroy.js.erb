// 削除した本の行を画面から消す
$("#book-<%= @book.id %>").remove();

<tr id="book-<%= book.id %>">
  <td><%= book.title %></td>
  <td><%= link_to 'Destroy', book, method: :delete, remote: true, data: { confirm: '本当に消しますか？' }, class: "btn btn-sm btn-danger" %></td>
</tr>